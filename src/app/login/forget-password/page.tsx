import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Perdeu a senha | Social Sync",
  description: "Recupere sua senha!",
};

export default async function ForgetPasswordPage() {
  return (
    <main>
      <h1>ForgetPasswordPage</h1>
    </main>
  );
}
